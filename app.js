function sum(numOne, numTwo){
  var sum = numOne + numTwo;
  return '"The sum of ' + numOne + ' and ' + numTwo + ' is ' + sum + '."';
}

function multiply(numOne, numTwo){
  var multiply = numOne * numTwo;
  return '"The product of ' + numOne + ' and ' + numTwo + ' is ' + multiply + '."';
}
